# Test Log

Date: 2026-06-24

## Commands

```bash
make test
make test-unit
make check-study
```

## Results

- `make test`: passed, `108` tests, `OK`.
- `make test-unit`: passed, `108` tests, `OK`.
- `make check-study`: passed.

## Note

`make test` and `make test-unit` now both use the offline-safe unit-test path.
